import { ButtonOption } from "../../../atom/Button";

type TextinputProps = {
  name: string;
  label?: string;
  placeholder?: string;
  value?: string;
  onChange?: (e: any) => void;
};

export const TextInput = ({ name, value, onChange }: TextinputProps) => {
  return <></>;
};
